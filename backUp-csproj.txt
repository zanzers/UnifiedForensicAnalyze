<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <OutputPath>_mmmn\</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>

    <SelfContained>false</SelfContained>
    <PublishTrimmed>false</PublishTrimmed>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="DocumentFormat.OpenXml" Version="3.3.0" />
    <PackageReference Include="System.Drawing.Common" Version="9.0.9" />
    <PackageReference Include="OpenCvSharp4" Version="4.11.0.20250507" />
    <PackageReference Include="OpenCvSharp4.Extensions" Version="4.11.0.20250507" />
    <PackageReference Include="OpenCvSharp4.runtime.win" Version="4.11.0.20250507" />

    <!-- Copy Python scripts and uploads -->
    <None Include="Py\**" CopyToOutputDirectory="PreserveNewest" />
    <None Include="uploads\**" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!-- Flatten OpenCvSharp native DLLs -->
  <Target Name="FlattenRuntimes" AfterTargets="Build">
    <ItemGroup>
      <NativeDlls Include="$(OutputPath)runtimes\win-x64\native\*.dll" />
    </ItemGroup>
    <Copy SourceFiles="@(NativeDlls)" DestinationFolder="$(OutputPath)" SkipUnchangedFiles="true" />
    <RemoveDir Directories="$(OutputPath)runtimes" />
  </Target>

</Project>

<!-- Note: Change tools and cnn_model path before build -->
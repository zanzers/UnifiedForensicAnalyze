import sys
import os



# Marcelot property 
















if __name__ == "__main__":
    video_path = sys.argv[1] if len(sys.argv) > 1 else "no_input"
    print(f"[LSTM TEST] Received input path: {video_path}")
    print(f"[LSTM TEST] This is just a test output.")
